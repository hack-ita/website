{
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.17",
    "@tailwindcss/typography": "^0.5.19",
    "@types/node": "^25.0.3",
    "tailwindcss": "^4.1.17"
  },
  "scripts": {
    "build:admin": "npx tina build",
    "build:hugo": "node scripts/copyImages.js && git config core.quotepath false && hugo --gc --minify --baseURL \"$URL\"",
    "build": "npm run build:admin && npm run build:hugo"
  },
  "dependencies": {
    "fs": "^0.0.1-security",
    "fse": "^4.0.1",
    "path": "^0.12.7",
    "tinacms": "^3.1.1",
    "@tinacms/cli": "^1.11.0"
  }
}